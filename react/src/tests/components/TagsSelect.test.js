// @flow
import React from 'react'
import expect from 'expect'
import TagsSelect from '../../containers/TagsSelect'

describe('TagsSelect', () => {
  it('should exist', () => {
    expect(TagsSelect).toBeDefined()
  })
})
