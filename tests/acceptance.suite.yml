# Codeception Test Suite Configuration
#
# Suite for acceptance tests.
# Perform tests in browser using the WebDriver or PhpBrowser.
# If you need both WebDriver and PHPBrowser tests - create a separate suite.

actor: AcceptanceTester
modules:
    enabled:
#        - Phalcon:
#            bootstrap: 'app/bootstrap/InitTest.php'
#            cleanup: true
#            savepoints: true
#            url: http://app
        - PhpBrowser:
            url: http://app
        - \SpreadShareTests\Helper\Acceptance
        - Db:
            dsn: 'mysql:host=mysqltest;dbname=spreadshare'
            user: 'spreadshare'
            password: 'spreadshare'
            populate: false
            cleanup: true
            reconnect: true
            dump: 'schemas/minimalDataForTesting.sql'